// @flow
import * as React from 'react';

type Props = {
  onChange: Function,
  currentState: Object,
};

const Button = (props: Props) => {
  return <button>test</button>;
};

export default Button;
